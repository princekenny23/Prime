"use client"

// Deprecated component placeholder to avoid compile errors.
// The real quick notifications modal has been removed.
export function NotificationsModal({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) {
  return null
}
